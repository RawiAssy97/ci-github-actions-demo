name: Basic Trigger On Push

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  show_repo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print CI message
        run: echo "Hello, CI with GitHub Actions!"
